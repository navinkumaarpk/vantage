diff --git a/log-mcp-server/src/main/java/io/vantage/logmcp/LogSearchTools.java b/log-mcp-server/src/main/java/io/vantage/logmcp/LogSearchTools.java
index 5e4543c..cca5204 100644
--- a/log-mcp-server/src/main/java/io/vantage/logmcp/LogSearchTools.java
+++ b/log-mcp-server/src/main/java/io/vantage/logmcp/LogSearchTools.java
@@ -4,7 +4,8 @@ import co.elastic.clients.elasticsearch.ElasticsearchClient;
 import co.elastic.clients.elasticsearch.core.SearchResponse;
 import co.elastic.clients.elasticsearch.core.search.Hit;
 import java.util.List;
-import org.springframework.ai.tool.annotation.McpTool;
+import org.springframework.ai.mcp.annotation.McpTool;
+import org.springframework.ai.mcp.annotation.McpToolParam;
 import org.springframework.stereotype.Component;
 
 /**
@@ -15,10 +16,11 @@ import org.springframework.stereotype.Component;
  * {@code @McpTool} specifically (per VISTA's prior design, which used
  * {@code @McpTool} for external MCP exposure and {@code @Tool} only when
  * also calling a method directly from an in-process ChatClient with no
- * network hop). {@code @McpTool}'s exact package
- * ({@code org.springframework.ai.tool.annotation.McpTool}) is a
- * best-effort guess, parallel to where {@code @Tool} lives — not yet
- * confirmed to compile.
+ * network hop). First attempt guessed the wrong package
+ * ({@code org.springframework.ai.tool.annotation.McpTool} — doesn't exist);
+ * correct package confirmed via Spring AI's official MCP reference docs:
+ * {@code org.springframework.ai.mcp.annotation.McpTool}, alongside
+ * {@code @McpToolParam} for per-parameter schema descriptions.
  */
 @Component
 public class LogSearchTools {
@@ -31,11 +33,11 @@ public class LogSearchTools {
         this.esClient = esClient;
     }
 
-    @McpTool(description = "Full-text and filtered search over OltMgr log lines. "
-            + "query: free-text match against the log message (optional). "
-            + "roltMac: exact R-OLT MAC address to filter by (optional). "
+    @McpTool(name = "search_logs", description = "Full-text and filtered search over OltMgr log lines. "
             + "Returns matching log entries, newest first, up to 50 results.")
-    public List<LogEntry> searchLogs(String query, String roltMac) {
+    public List<LogEntry> searchLogs(
+            @McpToolParam(description = "Free-text match against the log message", required = false) String query,
+            @McpToolParam(description = "Exact R-OLT MAC address to filter by", required = false) String roltMac) {
         try {
             SearchResponse<LogDocument> response = esClient.search(s -> s
                     .index(INDEX)
diff --git a/log-mcp-server/src/main/resources/application.yml b/log-mcp-server/src/main/resources/application.yml
index 0d6f4f1..5321f60 100644
--- a/log-mcp-server/src/main/resources/application.yml
+++ b/log-mcp-server/src/main/resources/application.yml
@@ -1,6 +1,11 @@
 spring:
   application:
     name: log-mcp-server
+  ai:
+    mcp:
+      server:
+        annotation-scanner:
+          enabled: true
 
 server:
   port: 8091

spring:
  application:
    name: log-analyzer-service
  ai:
    openai:
      api-key: not-needed
      base-url: http://192.168.1.107:8080
      chat:
        options:
          model: /opt/models/Qwen2.5-7B-Instruct-Q4_K_M.gguf
          max-tokens: 512

server:
  port: 8081

vantage:
  mcp:
    toolgroups:
      logs:
        url: http://192.168.1.108:8091/mcp    # was :8081 (log-analyzer-service's own
                                          # port) — real bug found earlier, fixed
                                          # locally but never actually pushed
      code:
        url: http://192.168.1.108:8092/mcp
